/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // assetPrefix: "/portfolio",
    // basePath: "/portfolio",
    // reactStrictMode: true,
};

export default nextConfig;
